# Plan Fase 32 — Filtros URL em 5 módulos (v1 rascunho)

**Spec:** `docs/superpowers/specs/2026-04-15-fase-32-filtros-url-5-modulos-v3.md`.

## Tasks (granularidade inicial)

- T1 — `useDebouncedValue` hook em `src/lib/hooks/` (se ausente).
- T2 — Products: action + schema + page + content + test.
- T3 — Tasks: novo `listTasks` + wrapper `listMyTasks` + schema + page + content + test.
- T4 — Campaigns: refactor + schema + page + content + test.
- T5 — Segments: refactor + schema + page + content + test.
- T6 — Workflows: refactor + schema + page + content + test.
- T7 — E2E Playwright specs (5).
- T8 — Verification + deploy + memory.
